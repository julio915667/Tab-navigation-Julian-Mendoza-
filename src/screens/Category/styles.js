import { StyleSheet, StatusBar } from "react-native";

export const styles = StyleSheet.create({
container:{
    backgroundColor: "#253746", flex: 1, paddingTop: StatusBar.currentHeight,
}, TextColor:{
    color: "#FFFFFF",
}, containerList:{
  
}
})